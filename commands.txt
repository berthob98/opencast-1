Build: 	mvn clean install -Dcheckstyle.skip -DskipTests -Pdev

Start: 	cd docs/scripts/devel-dependency-containers
	docker-compose up -d

	cd build/opencast-dist-develop-*
	./bin/start-opencast

Multi Node Setup:
cd scripts/devel-dependency-containers/                                                                                                                                               
sudo docker-compose -f docker-compose-postgresql.yml up -d

List all files in directory: find . '*' -type f -exec ls -ltrh {} \;
